# Planner Polina — environment variables
# Copy to .env and fill in values

# API authentication (optional — leave empty for dev)
API_TOKEN=

# Database
DATABASE_URL=sqlite+aiosqlite:////app/planner.db
